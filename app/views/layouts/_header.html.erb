<header>

<div class="container">
	<% unless current_page?('/') %>
	   <div class="logo"><%= link_to_unless_current("ALPHABLOG", root_path) %></div>
	<% else %>
		<h1><%= link_to_unless_current("ALPHABLOG", root_path) %></h1>
	<% end %>

	 <form class="navbar-form navbar-right" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
     </form>
     
	<%= render 'layouts/nav' %>
</div>

</header>